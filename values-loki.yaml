deploymentMode: SingleBinary

loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1

  storage:
    type: filesystem

singleBinary:
  replicas: 1

# Вимикаємо “розбиті” режими, нам не треба microservices
read:
  replicas: 0
write:
  replicas: 0
backend:
  replicas: 0
